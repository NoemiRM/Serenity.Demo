 #
 # This section defines environment-specific configuration for different environments.
 #

 environments {
   default {
     webdriver.base.url = "https://demo.evershop.io"
   }
   all {
     home.page = "#{webdriver.base.url}/"
   }
 }